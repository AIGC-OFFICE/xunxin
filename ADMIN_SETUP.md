# 循鑫管理后台 — 部署 & 使用指南（腾讯云 EdgeOne Pages）

> 这份文档专为非技术使用者准备。按顺序操作，5–10 分钟即可完成首次启用。

## 一、它能做什么

进 `https://wwwxunxinai.com/admin/`，登录后可以：

1. **更换首页 Hero 轮播图** — 3 张大图，点一下即可上传新图，立即生效
2. **更换 6 大业务模块的展示图** — 每类 3 张，共 18 张
3. **发布行业洞察文章** — 标题/正文（Markdown）/封面/分类，发布后自动出现在首页"行业洞察"，URL 形如 `/post/<slug>`

老版本网站里已有的 4 篇文章（金属纯度识别、拆除安全、企业合规、价格趋势）保持原样不变。后台只管理新增内容。

---

## 二、首次启用（一次性配置）

需要在腾讯云 EdgeOne Pages 控制台完成 3 件事，全免费。

### 1. 创建 KV 命名空间（存图片 + 文章）

> 后台需要一个共享的 KV 命名空间，存放管理员上传的图片和发布的文章。一个就够了。

1. 打开 [EdgeOne Pages 控制台](https://console.tencentcloud.com/edgeone/pages?tab=projects)
2. 左侧菜单点击 **KV 存储**（如果没看到，可能需要先开通；首次开通免费）
3. 点 **新建命名空间**：
   - **命名空间名称**：随便填，比如 `xunxin-data`
   - **创建** 完成
4. 进入刚创建的命名空间，点 **关联项目** 标签
5. 选择项目：你的 `xunxin` Pages 项目
6. **变量名（这个最关键）**：必须填 `XUNXIN_KV`（**全大写**，必须和这个一字不差）
7. **保存**

### 2. 设置环境变量（密码 + 签名密钥）

1. 在 EdgeOne Pages 控制台进入你的 `xunxin` 项目
2. 左侧或顶部找到 **设置** → **环境变量**
3. 添加 2 个变量：

| 变量名 | 值 | 类型 |
|---|---|---|
| `ADMIN_PASSWORD` | 你自己的强密码（≥ 12 位） | Secret（敏感） |
| `SESSION_SECRET` | 任意一长串随机字符（≥ 32 位） | Secret（敏感） |

> 生成随机字符串：打开 https://www.random.org/strings/ 选 1 string、长度 40、Alphanumeric 即可。
> 两个值都务必勾选"Secret/敏感"——避免泄露在部署日志里。

### 3. 部署最新代码

1. 把项目当前代码 push 到 GitHub（或你之前用的 Git 仓库）
2. EdgeOne Pages 会自动触发部署（默认绑定 main 分支）
3. 等部署完成（控制台 **部署历史** 显示成功）

---

## 三、第一次登录验证

部署成功后浏览器打开：

**`https://wwwxunxinai.com/admin/`**

输入 `ADMIN_PASSWORD` → 进入后台。

如果提示"KV namespace XUNXIN_KV is not bound"——回去检查第一步：变量名一定要是 `XUNXIN_KV`（全大写）。

---

## 四、日常使用

### 更换图片

1. 顶部切换到 **Hero 图** 或 **业务图**
2. 业务图页面顶部点对应分类（例如"钢结构拆除与回收"）
3. 点击任意图片或"替换"按钮 → 选本地新图（jpg/png/webp/avif 都行）
4. **浏览器会自动压缩到 1MB 以内并转成 webp 上传**（EdgeOne 单次请求体上限 1MB）
5. 上传完成约 1–2 秒后页面会刷新预览
6. 已替换的图片会显示绿色"已替换"徽章；点"恢复"可换回原始图片

> **提示**：图片上线后浏览器/CDN 会缓存最多 5 分钟。访客看到的可能是稍早一点的版本。

### 发布文章

1. 切到 **文章** 标签 → 点右上角"**发布新文章**"
2. 必填项：
   - **标题**：例 _废铜分级与定价的 5 个关键标准_
   - **URL slug**：自动从标题生成，可改。**仅限英文小写、数字、连字符**，发布后不可改
   - **分类**：从 6 个预设里选
   - **封面图**：从下拉网格里点选一张
   - **正文**：用 Markdown 写，左侧编辑、右侧实时预览
3. 选填：副标题、SEO 描述、关键词、阅读分钟数（不填会自动估算）
4. 点 **发布** → 立即生效，几秒内首页"行业洞察"就会出现

文章 URL 格式：`https://wwwxunxinai.com/post/<你的-slug>`

### 编辑/删除已发布文章

文章列表每行右侧有"编辑"和删除按钮。**slug 一旦发布不可修改**（避免老链接 404）。

### Markdown 速查

```
# 一级标题
## 二级标题

普通段落，**加粗**，*斜体*，`代码片段`，[链接文字](https://example.com)

- 列表项 1
- 列表项 2

1. 编号项 1
2. 编号项 2

> 这是引用，可以放金句或重点提示

---

(三个连字符 = 分隔线)
```

---

## 五、安全说明

- 后台地址 `/admin/` 没有从首页链接出去，且加了 `noindex` 防止被搜索引擎收录
- 密码保护 + HttpOnly Cookie，登录态 8 小时过期
- 上传只允许 21 个固定槽位（hero ×3、业务 6 类 ×3），不能任意上传到其他位置
- 浏览器自动转码为 webp 并压缩到 1MB 以内
- 4 篇老文章的 slug 列入保留名单，无法被覆盖

如果怀疑密码泄露：到 EdgeOne 控制台把 `ADMIN_PASSWORD` 和 `SESSION_SECRET` 都重新设置一次，所有现有登录态立即失效。

---

## 六、容量与配额（一律免费额度内）

| 资源 | 免费额度 | 你的实际用量 |
|---|---|---|
| EdgeOne Pages 部署 | 每月若干次构建、流量按基础套餐 | 改图/发文章不占构建额度（直写 KV） |
| EdgeOne Edge Functions | 每月若干百万次调用 | 文章/图片浏览 1 次 ≈ 1 次调用 |
| EdgeOne KV 存储 | 1 GB / 命名空间，单 value 最大 25MB | 21 张图共约 20 MB |

不会产生费用。

---

## 七、常见问题

**Q: 上传图片报错"KV namespace XUNXIN_KV is not bound"**
A: KV 关联项目时变量名没填对。控制台进 KV 命名空间 → 关联项目 → 变量名必须是 `XUNXIN_KV`（全大写）。改完后可能需要触发一次重新部署。

**Q: 上传图片报错"压缩后仍超过 900 KB，请改用更小的原图"**
A: 极端高清原图（4K+）压缩后仍可能略超阈值。换成 2K 以内的原图即可。

**Q: 改了图但首页看到的还是旧的**
A: 浏览器/CDN 缓存。强制刷新：Windows 按 `Ctrl+F5`，Mac 按 `Cmd+Shift+R`。访客最迟 5 分钟内会看到新图。

**Q: 忘了管理员密码怎么办**
A: 去 EdgeOne 控制台重新设置 `ADMIN_PASSWORD` 即可。

**Q: 发文章后 `/post/<slug>` 打开是 404**
A: 部署可能还没完成；等 1–2 分钟。或检查 `XUNXIN_KV` 是否绑定在了"生产环境（production）"而不仅仅是"预览环境（preview）"。
